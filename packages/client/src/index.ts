import { GlobalConfiguration } from './interfaces/interfaces';

/**
 * @class Runner
 *
 * Responsible for tying together the whole flow of this onsite
 * script. Instantiated with the configuration object.
 */

export default class Runner {
  constructor(config: GlobalConfiguration) {

  }
}
